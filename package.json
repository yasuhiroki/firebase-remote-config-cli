{
  "name": "firebase-remote-config-cli",
  "description": "Management firebase remote conig CLI tool",
  "main": "index.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "diff": "^5.1.0",
    "firebase-admin": "^11.4.1"
  }
}
